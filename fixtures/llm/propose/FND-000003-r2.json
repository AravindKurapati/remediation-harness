{
  "diff": "--- a/ledger/db.py\n+++ b/ledger/db.py\n@@ -34,6 +34,11 @@\n     return conn\n \n \n+#: The only column names that may reach an ORDER BY. The mapping returns its own\n+#: literal rather than the caller's key, so no caller string ever reaches the query.\n+SORTABLE_COLUMNS = {\"id\": \"id\", \"name\": \"name\", \"status\": \"status\", \"balance\": \"balance\"}\n+\n+\n def find_account_by_name(conn, name):\n     \"\"\"FINDING: SQL injection (CWE-89) - untrusted name concatenated into the query.\"\"\"\n     query = \"SELECT id, name, status, balance FROM accounts WHERE name = '\" + name + \"'\"\n@@ -54,7 +59,10 @@\n \n     A different shape: a placeholder binds values, never column names, so the fix\n     here cannot be parameterization. It has to be an allowlist.\"\"\"\n-    query = \"SELECT id, name, status, balance FROM accounts ORDER BY \" + sort_column\n+    column = SORTABLE_COLUMNS.get(sort_column)\n+    if column is None:\n+        raise ValueError(f\"cannot sort accounts by {sort_column!r}\")\n+    query = f\"SELECT id, name, status, balance FROM accounts ORDER BY {column}\"\n     return conn.execute(query).fetchall()\n \n \n",
  "pattern_id": "sqli-python-identifier-allowlist",
  "test_name": "test_unknown_sort_column_is_refused",
  "explanation": "The reviewer was right: a placeholder binds a value and cannot name a column, so `ORDER BY ?` sorts every row by the same constant and the ordering silently disappears. Replaced with the approved allowlist pattern - the caller's string selects a key, and the query receives the allowlist's own literal.",
  "left_alone": [],
  "test_source": "\"\"\"Regression test for FND-000003 - identifier injection in list_accounts_sorted.\n\nRound 2. The first attempt used a placeholder, which cannot bind a column name.\n\"\"\"\n\nimport pytest\n\nfrom ledger import db\n\n\ndef conn():\n    return db.bootstrap(db.connect())\n\n\ndef test_unknown_sort_column_is_refused():\n    \"\"\"Unpatched, this injects straight into the ORDER BY clause.\"\"\"\n    with pytest.raises(ValueError):\n        db.list_accounts_sorted(conn(), \"id; DROP TABLE accounts\")\n\n\ndef test_permitted_column_still_sorts():\n    \"\"\"The fix must not break sorting - which is exactly what `ORDER BY ?` would have.\"\"\"\n    rows = db.list_accounts_sorted(conn(), \"balance\")\n    assert [r[\"balance\"] for r in rows] == [0, 100, 250]\n"
}