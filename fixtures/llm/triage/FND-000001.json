{
  "category": "injection",
  "cwe": "CWE-89",
  "root_cause": "The `name` parameter reaches find_account_by_name from an HTTP query string via service.account_lookup and is concatenated into the SELECT text, so input can change the statement's structure rather than only its data.",
  "confidence_score": 0.94,
  "reasoning": "The concatenation is on the same line as the SELECT and the caller in service.py reads it straight from request_params. Untrusted data reaching a statement's text is unambiguous here."
}