[pytest]
# Only the harness's own tests. `samples/` holds deliberately vulnerable target
# projects with their own suites; those run inside a validation gate, against a
# patched scratch copy, not as part of this suite.
testpaths = tests
python_files = test_*.py
addopts = -q
